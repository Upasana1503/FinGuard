fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
pydantic[email]==2.10.4
python-dotenv==1.0.1

# ML stack -- CPU-only torch build (see Dockerfile: installed via the
# PyTorch CPU wheel index) since free hosting tiers have no GPU.
transformers==4.47.1
scikit-learn==1.6.1
numpy==1.26.4
joblib==1.4.2
